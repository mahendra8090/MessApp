<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hp.messapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AfterloginActivity" />
        <activity android:name=".AftermealActivity" />
        <activity android:name=".AfterrecordActivity" />
        <activity android:name=".Aftertotal_moneyActivity" />
        <activity android:name=".After_suggActivity" />
        <activity android:name=".After_feedbackActivity" />
        <activity android:name=".Aftermeal_dayActivity" />
        <activity android:name=".displaymenuActivity" />
        <activity android:name=".display_recordActivity"></activity>
    </application>

</manifest>